---
phase: 23-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - obsidian-plugin/main.ts
  - src/templates/registry.ts
autonomous: true

must_haves:
  truths:
    - "Plugin uses template registry instead of hardcoded FRONTMATTER_TEMPLATES"
    - "Template initialization pattern is shared between CLI and plugin (no duplication)"
    - "Plugin frontmatter output is byte-identical to current behavior"
  artifacts:
    - path: "obsidian-plugin/main.ts"
      provides: "Plugin using registry for frontmatter templates"
      contains: "templateRegistry"
    - path: "src/templates/registry.ts"
      provides: "Frontmatter template generation from entity type configs"
      exports: ["templateRegistry", "TemplateRegistry"]
  key_links:
    - from: "obsidian-plugin/main.ts"
      to: "src/templates/registry.ts"
      via: "import templateRegistry, build frontmatter from entityType.fields"
      pattern: "templateRegistry\\.getEntityType"
---

<objective>
Deduplicate FRONTMATTER_TEMPLATES from the Obsidian plugin by deriving frontmatter templates from the template registry's entity type configs.

Purpose: Eliminates 150+ lines of duplicated template data (DEBT-01) and unifies initialization (DEBT-02), unblocking Phase 27 graph view.
Output: Plugin imports from registry; no hardcoded FRONTMATTER_TEMPLATES constant.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/templates/registry.ts
@src/templates/types.ts
@src/templates/builtin/worldbuilding.ts
@obsidian-plugin/main.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontmatter template builder to registry</name>
  <files>src/templates/registry.ts, src/templates/types.ts</files>
  <action>
Add a method `buildFrontmatterTemplate(entityTypeName: string): Record<string, unknown>` to TemplateRegistry that:
1. Gets the entity type config from the active template via `getEntityType(name)`
2. Builds a frontmatter object with base fields: `{ id: '', type: entityTypeName, status: 'draft', title: '', importance: 'minor', tags: [], aliases: [] }`
3. Iterates `entityType.fields` and adds each field with its default value (use `field.default` if defined, otherwise derive from field type: string='', number=null, array=[], object with sub-fields=nested object)
4. Also add `buildAllFrontmatterTemplates(): Record<string, Record<string, unknown>>` that returns a map of all entity type names to their frontmatter templates

CRITICAL: The output must match the current FRONTMATTER_TEMPLATES structure in obsidian-plugin/main.ts exactly. Compare the worldbuilding.ts entity type definitions against the hardcoded templates to verify field-by-field. Pay special attention to:
- `character.appearance` is a nested object with specific string fields
- `character.personality` has `traits: [], motivations: [], flaws: []`
- `character.background` has `birthplace: '', occupation: '', affiliations: []`
- `asset` type has no `importance` field
- `relationships: []` and `assets: []` appear on some types

If the worldbuilding template definition in `src/templates/builtin/worldbuilding.ts` does NOT contain enough field metadata to reproduce the exact frontmatter structure, you must add the missing field definitions there. The worldbuilding template is the source of truth — make it complete.
  </action>
  <verify>
Write a quick test or script that calls `buildAllFrontmatterTemplates()` and deep-equals the result against the current hardcoded FRONTMATTER_TEMPLATES. Run `npm test` to ensure no regressions.
  </verify>
  <done>Registry can generate frontmatter templates identical to the hardcoded constant.</done>
</task>

<task type="auto">
  <name>Task 2: Replace plugin FRONTMATTER_TEMPLATES with registry calls</name>
  <files>obsidian-plugin/main.ts</files>
  <action>
1. Remove the entire `FRONTMATTER_TEMPLATES` constant (lines ~49-200) from obsidian-plugin/main.ts
2. Replace all 4 usage sites (lines ~603, ~720, ~1072, ~1135) with calls to the registry:
   - Where code does `FRONTMATTER_TEMPLATES[entityType]`, replace with `templateRegistry.buildFrontmatterTemplate(entityType)` (or equivalent using the method from Task 1)
3. The plugin already imports `templateRegistry` and initializes it in `onload()`. Verify the registry is initialized before any frontmatter template usage.
4. Ensure the template initialization in `onload()` (register + activate worldbuilding) is the single shared pattern — no duplicate initialization elsewhere.

Do NOT change any user-facing behavior. The frontmatter inserted into new notes must be identical.
  </action>
  <verify>
Build the plugin: `cd obsidian-plugin && npm run build`. Verify no TypeScript errors. Grep for `FRONTMATTER_TEMPLATES` — should return zero results. Run `npm test` from project root.
  </verify>
  <done>Plugin has zero hardcoded frontmatter templates; all 4 callsites use registry. Build succeeds.</done>
</task>

</tasks>

<verification>
- `grep -r "FRONTMATTER_TEMPLATES" obsidian-plugin/` returns no results
- `npm test` passes
- `cd obsidian-plugin && npm run build` succeeds
- Registry's `buildAllFrontmatterTemplates()` output matches original constant structure
</verification>

<success_criteria>
1. FRONTMATTER_TEMPLATES constant removed from plugin
2. All 4 usage sites use registry method
3. Template initialization is single shared pattern (registry.register + activate in onload)
4. All tests pass, plugin builds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-cleanup/23-01-SUMMARY.md`
</output>
