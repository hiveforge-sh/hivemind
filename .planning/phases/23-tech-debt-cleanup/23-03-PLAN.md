---
phase: 23-tech-debt-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - tests/cli/init/detection.test.ts
  - tests/cli/init/output.test.ts
  - tests/cli/init/validators.test.ts
  - tests/cli/init/prompts.test.ts
  - tests/cli/init/wizard.test.ts
autonomous: true

must_haves:
  truths:
    - "cli/init modules have test coverage above 80% lines"
    - "Tests follow existing project patterns (Vitest, existing CLI test conventions)"
  artifacts:
    - path: "tests/cli/init/detection.test.ts"
      provides: "Tests for template auto-detection"
    - path: "tests/cli/init/output.test.ts"
      provides: "Tests for config file writing"
    - path: "tests/cli/init/validators.test.ts"
      provides: "Tests for path and config validation"
    - path: "tests/cli/init/prompts.test.ts"
      provides: "Tests for interactive prompts (mocked)"
    - path: "tests/cli/init/wizard.test.ts"
      provides: "Tests for wizard flow (mocked)"
  key_links:
    - from: "tests/cli/init/*.test.ts"
      to: "src/cli/init/*.ts"
      via: "direct imports or CLI execution"
      pattern: "import.*from.*cli/init"
---

<objective>
Add test coverage for all cli/init modules (currently 0% coverage) to achieve above 80% line coverage.

Purpose: DEBT-03 requires cli/init test coverage above 80% lines. These modules handle project initialization and are untested.
Output: Test files for all 6 cli/init modules with >80% line coverage.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-tech-debt-cleanup/23-01-SUMMARY.md
@src/cli/init/detection.ts
@src/cli/init/index.ts
@src/cli/init/output.ts
@src/cli/init/prompts.ts
@src/cli/init/validators.ts
@src/cli/init/wizard.ts
@tests/cli/validate-template.test.ts
@tests/cli/add-template.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for detection, validators, and output modules</name>
  <files>tests/cli/init/detection.test.ts, tests/cli/init/validators.test.ts, tests/cli/init/output.test.ts</files>
  <action>
These three modules are the most unit-testable (pure-ish functions with clear inputs/outputs).

1. Read each source file to understand its exports and logic
2. Follow existing test patterns from `tests/cli/validate-template.test.ts` and `tests/cli/add-template.test.ts`
3. For each module, test:

**detection.ts:** Template auto-detection from vault files
- Test with temp directories containing various file structures
- Test detection of known entity types
- Test empty/missing directories

**validators.ts:** Path and config validation
- Test valid and invalid paths
- Test config validation rules
- Test edge cases (empty strings, special characters)

**output.ts:** Config file writing
- Test writing config to temp directory
- Test file content matches expected format
- Mock `exec` from child_process if it's used for git init or similar
- Test error handling (write failures)

Use `vi.mock` for any I/O that shouldn't touch real filesystem. Use `tmp` directories for integration-style tests. Follow existing beforeEach/afterEach cleanup patterns.
  </action>
  <verify>
`npm test -- tests/cli/init/detection.test.ts tests/cli/init/validators.test.ts tests/cli/init/output.test.ts` all pass. Run coverage: `npx vitest run --coverage tests/cli/init/` and verify each module is above 80% lines.
  </verify>
  <done>detection.ts, validators.ts, output.ts each have >80% line coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for prompts, wizard, and index modules</name>
  <files>tests/cli/init/prompts.test.ts, tests/cli/init/wizard.test.ts</files>
  <action>
These modules involve interactive prompts and orchestration, requiring mocking.

1. Read each source file to understand exports and dependencies
2. For prompts.ts and wizard.ts:
   - Mock `@inquirer/prompts` (or whatever prompt library is used) with `vi.mock`
   - Test each prompt function returns expected shape
   - Test wizard flow with mocked prompt responses
   - Test error/cancellation paths

3. For index.ts (init command entry point):
   - If it's mostly orchestration (calls wizard, then output), test via mocked dependencies
   - Or test via CLI integration: `node dist/cli.js init --help` should show help
   - If index.ts is thin glue, coverage from wizard + output tests may cover it sufficiently

Focus on covering branching logic and error paths. Don't test prompt library internals — mock them and test YOUR logic around the responses.
  </action>
  <verify>
`npm test -- tests/cli/init/` all pass. Run `npx vitest run --coverage tests/cli/init/` — all cli/init modules above 80% lines. If index.ts is below 80%, add a focused test for its orchestration logic.
  </verify>
  <done>All cli/init modules (detection, output, prompts, validators, wizard, index) have >80% line coverage.</done>
</task>

</tasks>

<verification>
- `npm test` passes (all tests including new ones)
- `npx vitest run --coverage tests/cli/init/` shows all modules >80% line coverage
- No flaky tests (run twice to confirm)
</verification>

<success_criteria>
1. All 6 cli/init modules have test coverage above 80% lines
2. Tests follow existing project patterns (Vitest, describe/it blocks, temp dirs)
3. Interactive prompts properly mocked (no TTY dependency in CI)
4. All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-cleanup/23-03-SUMMARY.md`
</output>
