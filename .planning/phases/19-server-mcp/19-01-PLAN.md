---
phase: 19-server-mcp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/server.ts]
autonomous: true

must_haves:
  truths:
    - "server.ts has zero explicit any types"
    - "MCP tool handlers validate arguments with Zod .parse() not type assertions"
    - "All function signatures use explicit typed interfaces instead of any"
  artifacts:
    - path: "src/server.ts"
      provides: "MCP server with typed tool handlers"
      contains: "Record<string, unknown>"
  key_links:
    - from: "src/server.ts"
      to: "src/types/index.ts"
      via: "Zod schema imports for argument parsing"
      pattern: "Schema\\.parse"
---

<objective>
Replace all `any` types in server.ts with explicit typed interfaces and Zod validation.

Purpose: Eliminate type safety escape hatches in the MCP server layer (MCP-01 requirement).
Output: server.ts with zero `any` types, using typed argument interfaces and Record<string, unknown> patterns.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-server-mcp/19-RESEARCH.md

@src/server.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all any types in server.ts</name>
  <files>src/server.ts</files>
  <action>
Replace every `any` type in server.ts with proper typed alternatives. The research identified these specific locations:

1. **Line 590, 655: `fields: any[]`** - Replace with explicit field interface (e.g., `FieldConfig[]` from templates/types or define a local `ToolField` interface with known properties like `{ name: string; type: string; description?: string }`).

2. **Line 661: `filters: any`** - Replace with `Record<string, unknown>` and add type narrowing where filter properties are accessed.

3. **Line 697: `filters?: any`** - Same pattern: `filters?: Record<string, unknown>`.

4. **Line 920: `results: any`** - Replace with the actual return type from the database/search query. Use the typed interfaces from Phase 18 (e.g., `SearchResult[]` or `QueryResult`). Check what the function actually returns and type it.

5. **Lines 1079, 1103, 1113, 1194, 1265: `as any` casts** - Remove each cast by fixing the underlying type mismatch. Common fixes:
   - If casting to satisfy an interface, add the correct type to the variable declaration
   - If casting database results, use the SQLite row interfaces from Phase 18
   - If casting for MCP SDK compatibility, use `Record<string, unknown>` or the SDK's own types

6. **Line 1243: `params: any[]`** - Replace with explicit parameter type array (e.g., `params: unknown[]` or `params: (string | number | null)[]` depending on what SQLite accepts).

7. **Line 1307: `Record<string, any[]>`** - Replace with `Record<string, unknown[]>` or define an explicit interface for the grouped data.

For each replacement:
- Do NOT use type assertions (`as`) to silence errors — fix the actual types
- Use `unknown` with type narrowing when the true type is unknowable
- Import existing types from `src/types/index.ts` and `src/templates/types.ts` where available
- Use Zod `.parse()` for MCP tool argument validation where Zod schemas already exist
- Add type guards or narrowing where `unknown` values are accessed

Run `npx tsc --noEmit` after changes to verify no type errors introduced.
  </action>
  <verify>
1. `npx tsc --noEmit` passes with no errors
2. `grep -n "any" src/server.ts` shows zero explicit `any` types (excluding comments and string literals like "any")
3. `npx eslint src/server.ts --rule '{"@typescript-eslint/no-explicit-any": "error"}' --no-eslintrc` reports zero warnings (or use project eslint config)
4. `npm test` passes — no regressions
  </verify>
  <done>server.ts contains zero explicit `any` types. All tool handlers use typed argument interfaces. All function signatures use explicit types. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep -c ": any\b" src/server.ts` returns 0
- `npm test` passes with no failures
</verification>

<success_criteria>
- MCP-01 satisfied: server.ts has zero any types
- Tool handlers use Zod .parse() for argument validation
- No type assertions (as any) remain
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-server-mcp/19-01-SUMMARY.md`
</output>
