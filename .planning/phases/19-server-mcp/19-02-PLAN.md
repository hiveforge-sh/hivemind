---
phase: 19-server-mcp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/mcp/tool-generator.ts]
autonomous: true

must_haves:
  truths:
    - "tool-generator.ts has zero explicit any types"
    - "Dynamic tool generation uses proper generics with EntityTypeConfig constraints"
    - "Query results use explicit interfaces instead of any"
  artifacts:
    - path: "src/mcp/tool-generator.ts"
      provides: "Type-safe dynamic tool generation"
      contains: "Record<string, unknown>"
  key_links:
    - from: "src/mcp/tool-generator.ts"
      to: "src/types/index.ts"
      via: "GraphNode and entity type imports"
      pattern: "GraphNode|EntityTypeConfig"
---

<objective>
Replace all `any` types in tool-generator.ts with explicit interfaces and generic constraints.

Purpose: Eliminate type safety escape hatches in dynamic tool generation (MCP-02 requirement).
Output: tool-generator.ts with zero `any` types, using proper generics for dynamic tool creation.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-server-mcp/19-RESEARCH.md

@src/mcp/tool-generator.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all any types in tool-generator.ts</name>
  <files>src/mcp/tool-generator.ts</files>
  <action>
Replace every `any` type in tool-generator.ts with proper typed alternatives. The research identified these specific locations:

1. **Line 19: `Record<string, any>`** - Replace with `Record<string, unknown>`. This is likely a properties object for graph nodes. Use `unknown` and add type narrowing where values are accessed.

2. **Line 186: `result: { node: any; relationships?: any[]; relatedNodes: any[] }`** - Define an explicit `QueryResult` interface (or import one if it exists):
   ```typescript
   interface QueryResult {
     node: GraphNode;
     relationships?: GraphEdge[];
     relatedNodes: GraphNode[];
   }
   ```
   Import `GraphNode` and `GraphEdge` from types/index.ts or graph module. Check what the actual data shapes are.

3. **Line 237: `Map<string, any>`** - Replace with `Map<string, unknown>` or `Map<string, GraphNode>` depending on what the map stores. Check usage context.

4. **Line 276: `Record<string, any[]>`** - Replace with `Record<string, unknown[]>` or `Record<string, GraphNode[]>` if grouping nodes.

5. **Line 285: `n: any`** - Replace with the actual type of what `n` is (likely `GraphNode` or a database row interface). Check the array being iterated.

6. **Line 307: `nodes: any[]`** - Replace with `nodes: GraphNode[]` or the appropriate typed array.

For each replacement:
- Import existing types from `src/types/index.ts` (GraphNode, GraphEdge, EntityTypeConfig, etc.)
- Use `Record<string, unknown>` instead of `Record<string, any>` per project convention
- Define local interfaces for complex shapes not already in types/index.ts
- Do NOT use `as` type assertions to silence errors
- Add type narrowing (typeof checks, instanceof, Array.isArray) where unknown values are accessed
- Use EntityTypeConfig from templates/types.ts for entity type parameters

Run `npx tsc --noEmit` after changes to verify no type errors introduced.
  </action>
  <verify>
1. `npx tsc --noEmit` passes with no errors
2. `grep -n "any" src/mcp/tool-generator.ts` shows zero explicit `any` types (excluding comments and string literals)
3. `npx eslint src/mcp/tool-generator.ts --rule '{"@typescript-eslint/no-explicit-any": "error"}' --no-eslintrc` reports zero warnings
4. `npm test` passes — no regressions
  </verify>
  <done>tool-generator.ts contains zero explicit `any` types. Dynamic tool generation uses proper generics with EntityTypeConfig constraints. Query results use explicit GraphNode/GraphEdge interfaces. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep -c ": any\b" src/mcp/tool-generator.ts` returns 0
- `npm test` passes with no failures
</verification>

<success_criteria>
- MCP-02 satisfied: tool-generator.ts has zero any types
- Dynamic tool generation uses proper generics
- All entity/query types use explicit interfaces
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-server-mcp/19-02-SUMMARY.md`
</output>
