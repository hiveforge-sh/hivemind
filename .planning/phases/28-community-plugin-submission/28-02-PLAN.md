---
phase: 28-community-plugin-submission
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .releaserc.json
  - obsidian-plugin/manifest.json
autonomous: true

must_haves:
  truths:
    - "Release artifacts include main.js, manifest.json, AND styles.css"
    - "manifest.json passes Obsidian validation rules (no 'obsidian' or 'plugin' in ID/name)"
    - "All production dependencies are MIT or Apache-2.0 compatible (no GPL/AGPL)"
    - "Release tag format matches manifest version format (both without 'v' prefix) (PLUG-03)"
  artifacts:
    - path: ".releaserc.json"
      provides: "Release configuration with all required Obsidian plugin assets"
      contains: "styles.css"
    - path: "obsidian-plugin/manifest.json"
      provides: "Valid plugin manifest"
      contains: "hivemind-mcp"
  key_links:
    - from: ".releaserc.json"
      to: "obsidian-plugin/styles.css"
      via: "GitHub release asset"
      pattern: "obsidian-plugin/styles.css"
---

<objective>
Validate and fix release configuration to ensure GitHub releases include all required Obsidian plugin files, and verify license compliance for community plugin submission.

Purpose: Obsidian community plugin validation requires main.js, manifest.json, AND styles.css as individual release assets. The current .releaserc.json is missing styles.css. Additionally, GPL/AGPL dependencies would disqualify the plugin from community listing.

Output: Updated .releaserc.json with all required assets, validated manifest.json, and license compliance verification.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-community-plugin-submission/28-RESEARCH.md
@.releaserc.json
@obsidian-plugin/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add styles.css to release assets</name>
  <files>.releaserc.json</files>
  <action>
Update .releaserc.json to include styles.css in the GitHub release assets.

Find the `@semantic-release/github` plugin configuration and add styles.css to the assets array:

```json
{
  "path": "obsidian-plugin/styles.css",
  "label": "Obsidian Plugin - Styles"
}
```

The assets array should now contain:
1. obsidian-plugin/main.js (already present)
2. obsidian-plugin/manifest.json (already present)
3. obsidian-plugin/styles.css (NEW)

Do NOT change the tagFormat - the current `${version}` format (without "v" prefix) is intentional and the project has been using this consistently since version 3.0.1. Changing it would break version history continuity.
  </action>
  <verify>Run `grep -A5 "@semantic-release/github" .releaserc.json | grep "styles.css"`</verify>
  <done>styles.css is configured as a GitHub release asset in .releaserc.json</done>
</task>

<task type="auto">
  <name>Task 2: Validate manifest.json against Obsidian rules</name>
  <files>obsidian-plugin/manifest.json</files>
  <action>
VERIFICATION TASK - Confirm manifest.json meets all Obsidian community plugin validation requirements. The current manifest is expected to be valid; this task verifies and documents compliance.

**Required fields verification:**
- id: "hivemind-mcp" (valid - no "obsidian" or "plugin" keywords)
- name: "Hivemind" (valid - no "Obsidian" or "Plugin" in name)
- version: Must match semver format (currently "3.3.1" - valid)
- minAppVersion: Must be valid Obsidian version (currently "1.4.0" - valid)
- description: Must end with punctuation (currently ends with "." - valid)
- author: Must be present (currently "HiveForge" - valid)

**Expected outcome:** All fields pass validation. No changes needed.

**Do NOT change:**
- The plugin ID "hivemind-mcp" - this is already established and valid
- The name "Hivemind" - clean and valid
- The author "HiveForge" - consistent with GitHub org

Per RESEARCH.md: "KEEP existing 'hivemind-mcp' ID — it's already established in manifest.json and meets all validation rules."
  </action>
  <verify>Run validation: `node -e "const m=require('./obsidian-plugin/manifest.json'); console.log('ID:', m.id, '| Name:', m.name, '| Version:', m.version, '| Ends with punctuation:', /[.!?]$/.test(m.description))"`</verify>
  <done>manifest.json passes all Obsidian validation rules - all fields verified present and valid</done>
</task>

<task type="auto">
  <name>Task 3: Run license compliance check</name>
  <files>None (verification only)</files>
  <action>
Run license check on obsidian-plugin production dependencies to verify no GPL/AGPL contamination.

Execute in obsidian-plugin directory:
```bash
cd obsidian-plugin && npx license-checker --production --summary
```

Check output for any GPL, AGPL, LGPL, or copyleft licenses.

**Expected safe licenses:**
- MIT
- Apache-2.0
- ISC
- BSD-2-Clause
- BSD-3-Clause
- 0BSD
- Unlicense
- CC0-1.0

**Rejection criteria:**
- GPL-2.0, GPL-3.0
- AGPL-3.0
- LGPL (without "linking exception")

If any GPL/AGPL licenses found, document which packages and their purposes for potential replacement.

Per RESEARCH.md: "All obsidian-plugin dependencies verified as MIT or MIT-compatible (Apache-2.0)."
  </action>
  <verify>License check output shows no GPL/AGPL in production dependencies</verify>
  <done>All production dependencies have MIT-compatible licenses (MIT, Apache-2.0, ISC, BSD)</done>
</task>

<task type="auto">
  <name>Task 4: Verify release tag format matches manifest version format</name>
  <files>None (verification only)</files>
  <action>
VERIFICATION TASK (PLUG-03) - Confirm .releaserc.json tagFormat matches manifest.json version format.

**Verify both use NO "v" prefix:**
1. Check .releaserc.json `tagFormat` field - should be `"${version}"` (not `"v${version}"`)
2. Check manifest.json version field - should be bare semver like `"3.3.1"` (not `"v3.3.1"`)

**Why this matters:**
Obsidian community plugins require release tags to exactly match manifest.json version values. The project established a no-prefix convention starting with version 3.0.1, and all subsequent releases follow this pattern.

**Expected outcome:** Both use bare semver without "v" prefix. No changes needed.

Run verification:
```bash
node -e "const r=require('./.releaserc.json'); const m=require('./obsidian-plugin/manifest.json'); console.log('tagFormat:', r.tagFormat); console.log('manifest version:', m.version); console.log('Match (both no v prefix):', r.tagFormat === '\${version}' && !m.version.startsWith('v'))"
```
  </action>
  <verify>Verification script outputs "Match (both no v prefix): true"</verify>
  <done>Confirmed .releaserc.json tagFormat ("${version}") and manifest.json version format are aligned - both use bare semver without "v" prefix</done>
</task>

</tasks>

<verification>
1. styles.css in release config: `grep "styles.css" .releaserc.json`
2. manifest.json valid: `node -e "const m=require('./obsidian-plugin/manifest.json'); if(!m.id || !m.name || !m.version || !m.description || !m.author) process.exit(1)"`
3. No GPL in licenses: `cd obsidian-plugin && npx license-checker --production --summary | grep -i gpl` (should return nothing)
4. Tag format matches: `node -e "const r=require('./.releaserc.json'); if(r.tagFormat !== '\${version}') process.exit(1)"` (PLUG-03)
</verification>

<success_criteria>
1. .releaserc.json includes styles.css in GitHub release assets
2. manifest.json passes all Obsidian validation rules
3. No GPL/AGPL dependencies in production bundle
4. Release tag format matches manifest version format (PLUG-03)
5. Ready for next release to include all required plugin files
</success_criteria>

<output>
After completion, create `.planning/phases/28-community-plugin-submission/28-02-SUMMARY.md`
</output>
