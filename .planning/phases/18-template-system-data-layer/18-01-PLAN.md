---
phase: 18-template-system-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/schema-factory.ts
  - src/templates/loader.ts
autonomous: true

must_haves:
  truths:
    - "schema-factory.ts has zero `any` types — all Zod schema operations use z.ZodRawShape generics"
    - "loader.ts has zero `any` types — configContent uses `unknown` with type guards"
    - "z.any() replaced with z.unknown() for dynamic field values"
    - "z.literal(config.name as any) cast removed"
  artifacts:
    - path: "src/templates/schema-factory.ts"
      provides: "Type-safe Zod schema factory"
      contains: "z.ZodObject<z.ZodRawShape>"
    - path: "src/templates/loader.ts"
      provides: "Type-safe template config loader"
      contains: "configContent: unknown"
  key_links:
    - from: "src/templates/schema-factory.ts"
      to: "z.ZodObject<z.ZodRawShape>"
      via: "Generic constraint on all schema return types and caches"
      pattern: "ZodObject<z\\.ZodRawShape>"
---

<objective>
Replace all `any` types in template system files (schema-factory.ts, loader.ts) with strict Zod generics and proper unknown typing.

Purpose: Satisfies requirements TMPL-01 and TMPL-02 — zero `any` types in template schema operations and loader.
Output: Two files with zero `any` type warnings.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-template-system-data-layer/18-RESEARCH.md
@src/templates/schema-factory.ts
@src/templates/loader.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all `any` types in schema-factory.ts</name>
  <files>src/templates/schema-factory.ts</files>
  <action>
Make the following replacements in schema-factory.ts:

1. Replace `z.any()` with `z.unknown()` (3 occurrences):
   - Line 28: `z.record(z.string(), z.any())` → `z.record(z.string(), z.unknown())`
   - Line 79: `z.record(z.string(), z.any())` → `z.record(z.string(), z.unknown())`
   - Line 84: `z.any()` → `z.unknown()`

2. Replace `z.ZodObject<any>` with `z.ZodObject<z.ZodRawShape>` (7 occurrences):
   - Line 111: `createEntitySchema` return type
   - Line 135: `schemaCache` Map type
   - Line 146: `getSchema` return type
   - Line 166: `generateSchemas` return type
   - Line 167: local `schemas` Map type
   - Line 209: `InferEntityType` generic constraint

3. Remove unnecessary `as any` cast (1 occurrence):
   - Line 121: `z.literal(config.name as any)` → `z.literal(config.name)`

Total: 10 `any` occurrences → 0.
  </action>
  <verify>
Run: `npx tsc --noEmit` — no new type errors in schema-factory.ts.
Run: `grep -n "any" src/templates/schema-factory.ts` — only appears in comments or unrelated words, not as a type annotation.
  </verify>
  <done>schema-factory.ts has zero `any` type annotations. All Zod schema operations use `z.ZodRawShape` generics. `z.unknown()` used for dynamic field values.</done>
</task>

<task type="auto">
  <name>Task 2: Replace `any` type in loader.ts</name>
  <files>src/templates/loader.ts</files>
  <action>
Make one replacement in loader.ts:

1. Line 80: `let configContent: any` → `let configContent: unknown`

2. After the JSON.parse line, add a type guard before accessing properties:
   - If there's already a type guard/assertion after parsing, just change the `any` to `unknown`.
   - If not, add: `if (typeof configContent !== 'object' || configContent === null) { throw new Error('Config file must contain a JSON object'); }` before accessing properties on configContent.
   - Then cast to `Record<string, unknown>` when accessing properties.

Read the file first to confirm what comes after the JSON.parse to determine if additional type guards are needed.
  </action>
  <verify>
Run: `npx tsc --noEmit` — no new type errors in loader.ts.
Run: `grep -n ": any" src/templates/loader.ts` — no matches.
  </verify>
  <done>loader.ts has zero `any` type annotations. configContent uses `unknown` with proper type guards.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. `grep -rn ": any\|<any>\|as any\|z\.any" src/templates/schema-factory.ts src/templates/loader.ts` returns no matches (only comment/word matches like "any template")
3. Existing tests pass: `npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- TMPL-01: schema-factory.ts has zero `any` types
- TMPL-02: loader.ts has zero `any` types
- No new TypeScript compilation errors
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-template-system-data-layer/18-01-SUMMARY.md`
</output>
