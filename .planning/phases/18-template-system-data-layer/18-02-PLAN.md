---
phase: 18-template-system-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/graph/database.ts
  - src/graph/builder.ts
  - src/search/engine.ts
autonomous: true

must_haves:
  truths:
    - "database.ts has zero `any` types — all SQLite query results use typed row interfaces"
    - "builder.ts has zero `any` types — frontmatter casts use Record<string, unknown>"
    - "engine.ts has zero `any` types — search results use GraphNode[] and GraphEdge[]"
  artifacts:
    - path: "src/graph/database.ts"
      provides: "Type-safe SQLite query layer"
      contains: "interface NodeRow"
    - path: "src/graph/builder.ts"
      provides: "Type-safe graph builder"
      contains: "Record<string, unknown>"
    - path: "src/search/engine.ts"
      provides: "Type-safe search engine"
      contains: "GraphNode[]"
  key_links:
    - from: "src/graph/database.ts"
      to: "NodeRow, RelationshipRow interfaces"
      via: "SQLite result type assertions"
      pattern: "as NodeRow|as RelationshipRow"
    - from: "src/search/engine.ts"
      to: "src/graph/database.ts"
      via: "Typed query results flowing through search"
      pattern: "GraphNode\\[\\]|GraphEdge\\[\\]"
---

<objective>
Replace all `any` types in data layer files (database.ts, builder.ts, engine.ts) with explicit SQLite row interfaces, typed arrays, and proper Record types.

Purpose: Satisfies requirements DATA-01, DATA-02, DATA-03 — zero `any` types in database queries, graph builder, and search engine.
Output: Three files with zero `any` type warnings.
</objective>

<execution_context>
@C:\Users\Preston\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Preston\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-template-system-data-layer/18-RESEARCH.md
@src/graph/database.ts
@src/graph/builder.ts
@src/search/engine.ts
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SQLite row interfaces and replace `as any` casts in database.ts</name>
  <files>src/graph/database.ts</files>
  <action>
1. Define row interfaces near the top of the file (after imports, before class):

```typescript
interface NodeRow {
  id: string;
  type: string;
  status: string;
  title: string;
  content: string;
  frontmatter: string;  // JSON string
  file_path: string;
  created_at: number;
  updated_at: number;
}

interface RelationshipRow {
  id: number;
  source_id: string;
  target_id: string;
  rel_type: string | null;
  properties: string | null;  // JSON string or NULL
}

interface SearchResultRow {
  id: string;
  rank: number;
}

interface StatRow {
  type: string;
  count: number;
}

interface CountRow {
  count: number;
}
```

Read the file first to confirm exact column names match the CREATE TABLE statements.

2. Replace all `as any` casts with typed alternatives:
   - Line 204: `stmt.get(id) as any` → `stmt.get(id) as NodeRow | undefined`
   - Line 225: `stmt.all() as any[]` → `stmt.all() as NodeRow[]`
   - Line 245: `stmt.all(type) as any[]` → `stmt.all(type) as NodeRow[]`
   - Line 269: `stmt.all(nodeId, nodeId) as any[]` → `stmt.all(nodeId, nodeId) as RelationshipRow[]`
   - Line 293: `stmt.all(query, limit) as any[]` → `stmt.all(query, limit) as SearchResultRow[]`

3. Replace `Record<string, any>` in insertRelationship:
   - Line 182: `properties?: Record<string, any>` → `properties?: Record<string, unknown>`

4. Fix any property access that TypeScript flags after replacing `any` with typed interfaces (row property names may differ from the current access patterns — adjust the mapping code to match the interface).
  </action>
  <verify>
Run: `npx tsc --noEmit` — no new type errors in database.ts.
Run: `grep -n "as any\|: any" src/graph/database.ts` — only matches "has any nodes" comment, not type annotations.
  </verify>
  <done>database.ts has zero `any` type annotations. All SQLite query results use explicit row interfaces. insertRelationship uses Record&lt;string, unknown&gt;.</done>
</task>

<task type="auto">
  <name>Task 2: Replace `any` types in builder.ts and engine.ts</name>
  <files>src/graph/builder.ts, src/search/engine.ts</files>
  <action>
**builder.ts** — Replace 3 `as any` casts:
- Line 64: `note.frontmatter as any` → `note.frontmatter as Record<string, unknown>`
- Line 66: `frontmatter.relationships as any[]` → `frontmatter.relationships as Array<Record<string, unknown>>`
  (Then access properties with type narrowing: check `typeof rel.target === 'string'` etc.)
- Line 174: `n.frontmatter as any` → `n.frontmatter as Record<string, unknown>`

Read builder.ts first to see what properties are accessed on these casts, and ensure the replacement types support those property accesses (may need to define a small RelationshipEntry interface if the access pattern is complex).

**engine.ts** — Replace 7 `any` occurrences with GraphNode/GraphEdge types:
- Line 4: `nodes: any[]` → `nodes: GraphNode[]`
- Line 5: `relationships?: any[]` → `relationships?: GraphEdge[]`
- Line 36: `let nodes: any[]` → `let nodes: GraphNode[]`
- Line 93: `const relationships: any[]` → `const relationships: GraphEdge[]`
- Line 126: `node: any` → `node: GraphNode`
- Line 127: `relationships: any[]` → `relationships: GraphEdge[]`
- Line 128: `relatedNodes: any[]` → `relatedNodes: GraphNode[]`
- Line 161: `Promise<any[]>` → `Promise<GraphNode[]>`

Ensure GraphNode and GraphEdge are imported from `../types/index.js`. If they're not already imported, add the import.
  </action>
  <verify>
Run: `npx tsc --noEmit` — no new type errors in builder.ts or engine.ts.
Run: `grep -n ": any\|as any" src/graph/builder.ts src/search/engine.ts` — no matches.
  </verify>
  <done>builder.ts has zero `any` type annotations. engine.ts has zero `any` type annotations. All search results use GraphNode[] and GraphEdge[].</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no new errors
2. `grep -rn ": any\|<any>\|as any" src/graph/database.ts src/graph/builder.ts src/search/engine.ts` returns no matches
3. Existing tests pass: `npx vitest run --reporter=verbose`
</verification>

<success_criteria>
- DATA-01: database.ts has zero `any` types, SQLite results use typed interfaces
- DATA-02: builder.ts has zero `any` types
- DATA-03: engine.ts has zero `any` types, search results strictly typed
- No new TypeScript compilation errors
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/18-template-system-data-layer/18-02-SUMMARY.md`
</output>
