---
phase: 21-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/comfyui/client.test.ts
  - tests/search/engine.test.ts
  - tests/mcp/tool-generator.test.ts
  - tests/vault/watcher.test.ts
autonomous: true
backfilled: 2026-05-08

must_haves:
  truths:
    - "comfyui/client.ts unit coverage ≥ 95%"
    - "search/engine.ts unit coverage ≥ 95%"
    - "mcp/tool-generator.ts unit coverage = 100%"
    - "vault/watcher.ts unit coverage = 100%"
    - "Full test suite passes"
  artifacts:
    - path: "tests/comfyui/client.test.ts"
      provides: "executeWorkflow polling, WebSocket, error-handling coverage"
    - path: "tests/search/engine.test.ts"
      provides: "filter, query-building, edge-case coverage"
    - path: "tests/mcp/tool-generator.test.ts"
      provides: "relationship grouping, dedup, content-shape coverage"
    - path: "tests/vault/watcher.test.ts"
      provides: "lifecycle, debounce, file-event coverage"
---

<objective>
Raise unit-test coverage on four under-tested modules above the project bar
(≥95% statement coverage, 100% on the two smallest), so mutation testing in
phase 22 has meaningful signal to work with.

Targets:
- `comfyui/client.ts`: 62% → ≥95%
- `search/engine.ts`: 72% → ≥95%
- `mcp/tool-generator.ts`: 75% → 100%
- `vault/watcher.ts`: 47% → 100%
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Cover comfyui/client.ts</name>
  <files>tests/comfyui/client.test.ts</files>
  <action>
Add tests for `executeWorkflow` polling loops, WebSocket message handling, and the
error paths (timeouts, malformed responses, connection drops). Target ≥95% coverage.
  </action>
  <verify>vitest run --coverage src/comfyui/client.ts → ≥95%</verify>
  <done>client.ts coverage ≥95%; all new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Cover search/engine.ts</name>
  <files>tests/search/engine.test.ts</files>
  <action>
Add tests for filter combinations, query-building edge cases (empty filters, mixed
type/tag/date filters), and the boundary conditions exposed by graph traversal entry.
  </action>
  <verify>vitest run --coverage src/search/engine.ts → ≥95%</verify>
  <done>engine.ts coverage ≥95%; all new tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Cover mcp/tool-generator.ts to 100%</name>
  <files>tests/mcp/tool-generator.test.ts</files>
  <action>
Cover relationship grouping, deduplication, and content shape transformations.
This file is small enough to reach 100% with a focused test pass.
  </action>
  <verify>vitest run --coverage src/mcp/tool-generator.ts → 100%</verify>
  <done>tool-generator.ts coverage = 100%.</done>
</task>

<task type="auto">
  <name>Task 4: Cover vault/watcher.ts to 100%</name>
  <files>tests/vault/watcher.test.ts</files>
  <action>
Cover the watcher lifecycle (start/stop), debounce behavior, and each chokidar
file-event handler (add, change, unlink, addDir, unlinkDir).
  </action>
  <verify>vitest run --coverage src/vault/watcher.ts → 100%</verify>
  <done>watcher.ts coverage = 100%.</done>
</task>

</tasks>

<success_criteria>
1. Coverage targets met for all four modules
2. Full test suite passes (target met at 642 tests in original execution)
3. No flaky tests introduced
</success_criteria>

<backfill_note>
This PLAN.md was backfilled on 2026-05-08 from `21-SUMMARY.md`. Originally executed
ad-hoc and committed circa 2026-01-27.
</backfill_note>
