---
phase: 20-comfyui-cli-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/comfyui/client.ts
  - src/comfyui/workflow.ts
  - src/cli.ts
  - src/cli/init/index.ts
  - src/cli/fix/index.ts
  - src/cli/validate/index.ts
autonomous: true
backfilled: 2026-05-08

must_haves:
  truths:
    - "Zero explicit `any` types in src/comfyui/* and src/cli*"
    - "ESLint @typescript-eslint/no-explicit-any reports zero warnings across the codebase"
    - "All tests passing after axios→fetch migration"
  artifacts:
    - path: "src/comfyui/client.ts"
      provides: "Type-safe ComfyUI HTTP client using native fetch"
      contains: "fetch"
    - path: "src/comfyui/workflow.ts"
      provides: "Typed SQLite workflow row access"
      contains: "WorkflowRow"
---

<objective>
Eliminate all remaining explicit `any` types across the ComfyUI and CLI layers, then
enforce the rule via the existing `@typescript-eslint/no-explicit-any` lint rule so
regressions are caught at PR time.

Migrate `comfyui/client.ts` from axios to native `fetch` while doing so to drop a
runtime dependency that contributed weakly typed shapes.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Type-strict comfyui layer</name>
  <files>src/comfyui/client.ts, src/comfyui/workflow.ts</files>
  <action>
Replace axios with native `fetch` in `client.ts`, introducing typed API-response
interfaces. Add `WorkflowRow` interface for `workflow.ts` SQLite results. Eliminate
all explicit `any`.
  </action>
  <verify>grep -c '\bany\b' src/comfyui/client.ts src/comfyui/workflow.ts → 0 (excluding comments)</verify>
  <done>Both files compile with zero explicit `any` types and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Type-strict CLI layer</name>
  <files>src/cli.ts, src/cli/init/index.ts, src/cli/fix/index.ts, src/cli/validate/index.ts</files>
  <action>
Type the CLI config objects in `cli.ts`, parameters in `init/index.ts`, scanner
results in `validate/index.ts`, and switch catch clauses in `fix/index.ts` to
`unknown` with explicit narrowing.
  </action>
  <verify>grep -c '\bany\b' on each file → 0 (excluding comments)</verify>
  <done>All four CLI files compile with zero explicit `any` types.</done>
</task>

<task type="auto">
  <name>Task 3: Lint enforcement</name>
  <files>(no file changes — verification only)</files>
  <action>
Run `npm run lint` and confirm `@typescript-eslint/no-explicit-any` reports zero
warnings across the codebase. The rule should already be enabled from earlier
phases; this task ensures the codebase actually passes it now.
  </action>
  <verify>npm run lint → 0 no-explicit-any warnings</verify>
  <done>Lint clean; the rule will now block any new `any` introductions in PRs.</done>
</task>

</tasks>

<success_criteria>
1. Zero explicit `any` types in src/comfyui/* and src/cli*
2. axios removed; native fetch used for ComfyUI calls
3. ESLint passes with no `no-explicit-any` warnings
4. Tests pass
</success_criteria>

<backfill_note>
This PLAN.md was backfilled on 2026-05-08 from `20-SUMMARY.md`. The work was originally
executed ad-hoc (likely via `/gsd-quick`) and committed alongside phases 21–22 around
2026-01-27.
</backfill_note>
